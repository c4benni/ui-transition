import { Spring, SpringObject } from "../../props/types";
import { AnimPhase } from "../../types";
export default function extractSpring(propsEase: Spring, configEase: Spring | undefined, animPhase: AnimPhase): SpringObject;
