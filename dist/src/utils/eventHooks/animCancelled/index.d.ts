import { RendererElement } from "vue";
import { Emit } from "../../../types";
import { Hook } from "../types";
export default function animcancelled(el: RendererElement, hook: Hook, emit: Emit): void;
