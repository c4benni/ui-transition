import { SpringObject } from "../../props/types";
import { AnimPhase, BuildAnim } from "../../types";
export default function keyframeName(config: BuildAnim, spring: SpringObject, animPhase: AnimPhase): string;
