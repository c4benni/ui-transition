import { ConfigProp } from "../types/props/config";

export default {
  config: "scale(0.8, 1)" as ConfigProp,
  previousTransform: undefined as Boolean | undefined,
  group: undefined as Boolean | undefined,
  optimise: true as Boolean | undefined,
  prefix: "uit-" as string,
};
